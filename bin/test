#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

echo "==> Backend tests (apps/api)"
cd "$ROOT_DIR/apps/api"
bundle exec rails test

echo "==> Frontend tests (apps/web)"
cd "$ROOT_DIR/apps/web"
npm run test --if-present
